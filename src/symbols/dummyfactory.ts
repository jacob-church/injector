/**
 * A symbol for marking a class as eligible for serving with `getInjectionContext`.
 *
 * See `getInjectionContext` usage for more information.
 */
export const DummyFactory = Symbol("DummyFactory");
